# Backend (Phase 2)

Laravel + MySQL backend will live here.

Not implemented in Phase 1. The frontend mockup uses static data and service interfaces that will map to REST endpoints:

- `GET /api/v1/students`
- `GET /api/v1/students/{id}`
- `GET /api/v1/attendance`
- etc.
